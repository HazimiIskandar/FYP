const processSensorAlert = (
    senior_id,
    sensor_type
) => {

    console.log(
        `Sensor alert from Senior ${senior_id}: ${sensor_type}`
    );

};

module.exports = {
    processSensorAlert
};